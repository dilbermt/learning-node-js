// npm

// local dependancy - use only in this purticular project
//  npm i <packageName>

// global dependancy - use it in any project
// npm install -g <packageName>

//  package.json -- stores important info about the project / package including dependancies
//  the easiest way to create package.json is npm init -y

//  while pushing to git, to ignore specified files create a .gitignore file and add the modules there as /node_modules

// to start a project by someone else enter npm install and npm will install the dependancies
// install nodemon as a devdependancy. npm i nodemon -D

// set up dev script inside the dependanccies as  
// "start": "node app.js",
// "dev": "nodemon app.js"

// to run, npm run dev
